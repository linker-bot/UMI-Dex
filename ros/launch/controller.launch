<launch>
  <!-- Raw CAN frame publisher (no assembly/calibration — done offline). -->
  <arg name="can_channel" default="can0" />

  <node pkg="umi_dex" type="can_raw_node" name="can_raw"
        output="screen">
    <param name="can_channel" value="$(arg can_channel)" />
  </node>
</launch>
